import React from 'react'

const Button = ({bgColor, color, size, text, borderRadius}) => {
  return (
    <div>
      <button type='button' 
      style={{bac}}
      
      ></button>
    </div>
  )
}

export default Button